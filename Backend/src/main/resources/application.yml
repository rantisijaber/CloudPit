server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cloudpit_db
    username: postgres
    password: ${SPRING_DATA_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
